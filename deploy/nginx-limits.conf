# Rate limits for the First Brew app. Goes to /etc/nginx/conf.d/ (http context).
limit_req_zone $binary_remote_addr zone=firstbrew_api:10m rate=2r/s;
limit_req_zone $binary_remote_addr zone=firstbrew_ocr:10m rate=12r/m;
limit_conn_zone $binary_remote_addr zone=firstbrew_conn:10m;
